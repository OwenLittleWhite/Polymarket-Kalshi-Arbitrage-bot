几周前，我决定构建自己的 @Polymarket 机器人。我花了几周时间开发了一个完整的版本。
我投入这些时间是因为 Polymarket 上存在很多低效现象。是的，确实有一些机器人已经在利用这些低效现象，但还不够。机会远多于机器人数量。

## 机器人逻辑

机器人的逻辑基于我过去手动执行的一个策略，后来为了效率将其自动化。该机器人在"BTC 15分钟涨/跌"市场上运行。

机器人运行一个实时监控器，自动切换到当前的 BTC 15分钟轮次，通过 WebSocket 流式传输最优买卖价，显示固定的终端 UI，并允许通过文本命令进行全面控制。

**手动模式下**，你可以直接进行交易：
- `buy up <usd> / buy down <usd>` - 花费指定美元金额
- `buyshares up <shares> / buyshares down <shares>` - 使用 UI 友好的 LIMIT + GTC 订单以当前最优卖价购买精确数量的份额

**自动模式**运行一个重复的两腿循环：

1. **第一阶段**：仅在轮次开始的前 `windowMin` 分钟内监控价格变动。如果任一侧快速下跌（约 3 秒内下跌至少 `movePct`），则触发第一阶段并买入被抛售的一侧。

2. **第二阶段**：第一阶段后，机器人永远不会再次买入同一侧。它等待第二阶段（对冲），仅当满足以下条件时触发：`leg1EntryPrice + oppositeAsk <= sumTarget`

3. 当满足该条件时，它买入相反的一侧。第二阶段完成后，循环结束，机器人返回使用相同参数监控下一次抛售。

4. 如果在循环期间轮次发生变化，机器人将放弃未完成的循环，并在下一轮次使用相同设置重新开始。

**自动模式参数设置**：
```
auto on <shares> [sum=0.95] [move=0.15] [windowMin=2]
```

| 参数 | 说明 |
|------|------|
| `shares` | 两腿使用的仓位大小 |
| `sum` | 允许对冲的阈值 |
| `move` (movePct) | 抛售阈值（例如 0.15 = 15%）|
| `windowMin` | 从轮次开始到第一阶段允许的时间（分钟）|

## 回测

机器人的逻辑很简单：等待剧烈抛售，买入刚刚被抛售的一侧，然后等待稳定并通过对冲买入相反的一侧，同时保证：`priceUP + priceDOWN < 1`

但这个逻辑需要测试。它长期真的有效吗？

更重要的是，机器人有很多参数（shares、sum、movePct、windowMin 等）。哪些参数集是最优的并能最大化利润？

我的第一个想法是让机器人实时运行一周并观察结果。问题是这太长了，只能测试一个参数集，而我需要测试多个。

我的第二个想法是使用 Polymarket CLOB API 的在线历史数据进行回测。不幸的是，对于 BTC 15分钟涨/跌市场，历史端点一直返回空数据集。没有历史价格刻度，回测无法检测"约 3 秒内的抛售"，无法触发第一阶段，无论参数如何都会产生 0 个循环和 0% 的 ROI。

经过进一步调查，我意识到其他用户在检索某些市场的历史数据时也遇到了同样的问题。我测试了其他确实返回历史数据的市场，并得出结论，对于这个特定市场，根本没有保留历史数据。

由于这个限制，回测此策略的唯一可靠方法是在机器人运行时记录实时最优卖价来创建自己的历史数据集。

**记录器**将包含以下内容的快照写入磁盘：
- 时间戳
- 轮次 slug
- 剩余秒数
- 涨/跌代币 ID
- 涨/跌最优卖价

"记录回测"然后重放这些快照并确定性地应用相同的自动逻辑。这保证了访问检测抛售和对冲条件所需的高频数据。

总共 4 天内我收集了 6 GB 的数据。我可以记录更多，但我觉得这足以测试不同的参数集。

### 回测结果

**测试参数集 1（保守）：**
- 初始余额：$1,000
- 每笔交易 20 份
- sumTarget = 0.95
- 抛售阈值 = 15%
- windowMin = 2 分钟

我还应用了恒定的 0.5% 费率和 2% 价差，以保持保守情况。

回测显示 ROI 为 **86%**，$1,000 在几天内变成了 $1,869。

**测试参数集 2（激进）：**
- 初始余额：$1,000
- 每笔交易 20 份
- sumTarget = 0.6
- 抛售阈值 = 1%
- windowMin = 15 分钟

结果：2 天后 ROI 为 **-50%**。

这清楚地表明参数选择是最重要的因素。它可以让你赚很多钱，也可能导致重大损失。

## 回测局限性

即使包括费用和价差，回测也有局限性：

1. 只使用几天的数据，可能不足以获得完整的市场视图
2. 依赖记录的最优卖价快照；实际上，订单可能部分成交或以不同价格成交
3. 订单簿深度和可用成交量没有建模
4. 未捕捉秒内微观波动（数据每秒采样一次）。回测有 1 秒时间戳，但每秒之间可能发生很多情况
5. 回测中滑移是恒定的，不模拟可变延迟（例如 200-1500 ms）或网络峰值
6. 每条腿都被视为"即时"执行（无订单队列，无挂单）
7. 费用统一应用，而实际上它们可能取决于：
   - 市场/代币
   - 挂单方 vs 吃单方
   - 费用等级或条件

为了保持悲观，我应用了一个规则：如果第二阶段在市场收盘前没有执行，第一阶段被视为总损失。

这是故意保守的，但并不总是现实的：
- 有时第一阶段可以提前平仓
- 有时它最终价内（ITM）并获胜
- 有时损失可能是部分的而不是全部的

损失可能被高估，但这提供了一个有用的"最坏情况"场景。

最重要的是，回测不模拟你自己的市场影响。实际上，你的订单可以：
- 移动订单簿
- 吸引或排斥其他交易者
- 导致非线性滑移

回测假设你是一个没有影响的纯粹价格接受者。

最后，它不模拟速率限制、API 错误、订单拒绝、暂停、超时、重连，或机器人忙碌并错过信号的时刻。

回测对于识别良好的参数范围非常有价值，但不是 100% 的保证，因为一些现实效果无法建模。

## 基础设施

我计划在树莓派上运行机器人，以避免消耗我主机的资源并保持 24/7 运行。

但这可以显著改善：
- 使用 Rust 而不是 JavaScript 将提供更好的性能和处理时间
- 运行专用的 Polygon RPC 节点将进一步减少延迟
- 部署在靠近 Polymarket 服务器的 VPS 上也将显著减少延迟

肯定还有我没发现的其他优化。

目前，我正在学习 Rust，因为它正成为 Web3 开发的必备语言。

如果你对我的机器人感兴趣（其中包括回测框架），请私信我。
